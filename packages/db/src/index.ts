/**
 * Prisma schema, migrations, and seed live in this package.
 *
 * The client itself is not exported yet — it cannot be generated until the initial
 * migration exists (ticket P1-02). Application code reaches the database through
 * `apps/web/lib/db.ts`, never by importing Prisma directly. See docs/CLAUDE.md.
 */

export {};
